{
  "private": true,
  "workspaces": [
    "backend",
    "domain",
    "frontend"
  ],
  "scripts": {
    "build": "yarn workspace frontend build && yarn workspace backend build"
  },
  "dependencies": {
    "syncpack": "^12.3.0"
  }
}
